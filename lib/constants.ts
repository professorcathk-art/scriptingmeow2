/** Instagram caption / draft caption max length (aligned with product limits). */
export const MAX_IG_CAPTION_CHARS = 2000;

/** Content idea field max length in create-post and drafts. */
export const MAX_CONTENT_IDEA_CHARS = 2000;

/** Overall design brief: scene, objects, composition, on-image copy (draft Step 3). Text-heavy layouts need room for full copy decks. */
export const MAX_OVERALL_DESIGN_CHARS = 2500;

/** @deprecated use MAX_OVERALL_DESIGN_CHARS */
export const MAX_ON_IMAGE_INSTRUCTION_CHARS = MAX_OVERALL_DESIGN_CHARS;

/** Styling field: brand-aligned look (palette, mood, medium, light). */
export const MAX_STYLING_CHARS = 1500;
