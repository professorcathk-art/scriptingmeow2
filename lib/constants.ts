/** Instagram caption / draft caption max length (aligned with product limits). */
export const MAX_IG_CAPTION_CHARS = 2000;

/** Content idea field max length in create-post and drafts. */
export const MAX_CONTENT_IDEA_CHARS = 2000;

/** On-image brief: copy + placement instructions for the image model (draft Step 3). */
export const MAX_ON_IMAGE_INSTRUCTION_CHARS = 1500;
